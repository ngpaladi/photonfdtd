# Documentation build dependencies (mirrors the [docs] extra in pyproject.toml).
# CI installs the package itself with `pip install .[docs]`; this file exists so
# the docs can also be built standalone with `pip install -r docs/requirements.txt`.
sphinx>=7
sphinx-rtd-theme>=3
